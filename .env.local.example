# Next.js specific
# NEXT_PUBLIC_SOME_PUBLIC_VARIABLE=your_public_value

# Firebase (client-side configuration, typically as a JSON string)
# Ensure this is prefixed with NEXT_PUBLIC_ if used client-side directly
NEXT_PUBLIC_FIREBASE_CONFIG='{"apiKey": "AIza...", "authDomain": "project-id.firebaseapp.com", ...}'

# Firebase Admin SDK (server-side configuration, if used directly via Admin SDK initialized in Next.js API routes)
# These are typically not needed if using the client-side SDK or if the Admin SDK is auto-initialized in a serverless environment.
# FIREBASE_PROJECT_ID="your-project-id"
# FIREBASE_CLIENT_EMAIL="firebase-adminsdk-account@your-project-id.iam.gserviceaccount.com"
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Google Cloud
NEXT_PUBLIC_GOOGLE_API_KEY="AIza..." # For client-side Google APIs
GOOGLE_APPLICATION_CREDENTIALS="/path/to/your/service-account-file.json" # Or the JSON content itself for server-side

# Genkit related (if any specific ones are typically needed by Genkit flows)
# GENKIT_API_KEY="some_genkit_api_key"
# GENKIT_ENV="development" # Or "production"

# Other application-specific variables
# API_BASE_URL="http://localhost:3000/api"
# DATABASE_URL="your_database_connection_string"
